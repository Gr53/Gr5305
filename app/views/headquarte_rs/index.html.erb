<p id="notice"><%= notice %></p>

<h1>Headquarters</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Location</th>
      <th>Address</th>
      <th>Location map</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @headquarters.each do |headquarter| %>
      <tr>
        <td><%= headquarter.ID %></td>
        <td><%= headquarter.NAME %></td>
        <td><%= headquarter.LOCATION %></td>
        <td><%= headquarter.ADDRESS %></td>
        <td><%= headquarter.LOCATION_MAP %></td>
        <td><%= link_to 'Show', headquarter %></td>
        <td><%= link_to 'Edit', edit_headquarter_path(headquarter) %></td>
        <td><%= link_to 'Destroy', headquarter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Headquarter', new_headquarter_path %>
