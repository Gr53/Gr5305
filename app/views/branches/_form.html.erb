<%= form_with(model: branch, local: true) do |form| %>
  <% if branch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(branch.errors.count, "error") %> prohibited this branch from being saved:</h2>

      <ul>
      <% branch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :locality %>
    <%= form.text_field :locality %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :location_map %>
    <%= form.text_field :location_map %>
  </div>

  <div class="field">
    <%= form.label :institution_id %>
    
     <%= form.collection_select(:institution_id, @institutions, :id, :name, :include_blank => "porfa institucion") %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
